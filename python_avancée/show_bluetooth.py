from gui import AppGUI

guil=AppGUI()

class show_bluetooth:
    def show_bluetooth():
        guil.hide_all_buttons()

        guil.bannerButton8.place(x=25, y=75)
        guil.search_button.place(x=75,y=150)
        guil.appareil_label.place(x=100, y=200)
        guil.devices_listbox.place(x=50, y=250)
        guil.connect_button.place(x=150, y=450)
        
    
        pass